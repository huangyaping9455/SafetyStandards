server:
  port: 8100
#文件的物理路径前缀与url前缀
fileserver:
  path-prefix: ${fileserver.dev.path}
  url-prefix: ${fileserver.dev.url}
  falg: ${fileserver.dev.flag}
  photoLogo: ${fileserver.dev.photoLogo}
alarmserver:
  address-path: ${alarmserver.dev.address}
